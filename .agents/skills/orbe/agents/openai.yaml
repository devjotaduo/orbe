interface:
  display_name: "Orbe"
  short_description: "Repo-specific patterns and workflows for orbe"
  default_prompt: "Use the orbe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true